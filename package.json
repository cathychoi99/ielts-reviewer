{
  "name": "ielts-material-reviewer",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && node --import tsx src/index.ts"
  }
}
